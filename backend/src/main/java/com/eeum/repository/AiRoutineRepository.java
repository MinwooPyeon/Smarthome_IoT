package com.eeum.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.eeum.entity.AiRoutine;

public interface AiRoutineRepository extends JpaRepository<AiRoutine, Integer> {
}

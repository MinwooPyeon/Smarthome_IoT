server {
    listen 80;
    server_name _;

    location /floorplans/ {
        alias /usr/share/nginx/html/floorplans/;
        autoindex off;
        expires 7d;
        add_header Cache-Control "public";
        try_files $uri =404;
    }
}
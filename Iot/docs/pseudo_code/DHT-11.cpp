// =============================
// 초기 설정
// =============================
초기화:
    - GPIO 라이브러리 초기화 (예: wiringPi, pigpio 등)
    - DHT11 데이터 핀 설정
    - 무한 루프 시작 (예: 2초 주기)

// =============================
// DHT11 데이터 읽기 절차
// =============================
DHT11_Read():
    1. MCU → DHT11 시작 신호 보내기
        - 데이터 핀을 OUTPUT으로 설정
        - LOW 상태 유지 (약 18ms)
        - HIGH 상태로 전환 (약 40µs)
        - 데이터 핀을 INPUT 모드로 변경

    2. DHT11 응답 대기
        - DHT11에서 LOW → HIGH → LOW 신호를 보낼 때까지 대기
        - 타임아웃 발생 시 실패 처리

    3. 데이터 수신 (총 40비트)
        - 1비트 수신 절차:
            a. LOW 기간 대기 (시작 비트 신호)
            b. HIGH 기간 측정
            c. HIGH 길이에 따라 0 또는 1 저장
                * 약 26~28µs → 0
                * 약 70µs → 1
        - 이 과정을 40번 반복

    4. 40비트 → 5바이트 변환
        - data[0]: 습도 정수부
        - data[1]: 습도 소수부
        - data[2]: 온도 정수부
        - data[3]: 온도 소수부
        - data[4]: 체크섬

    5. 체크섬 검증
        - (data[0] + data[1] + data[2] + data[3]) & 0xFF == data[4]
        - 일치하면 데이터 유효, 아니면 실패 처리

    6. 성공 시 온도/습도 반환

// =============================
// 메인 루프
// =============================
main():
    DHT11 초기화
    반복:
        - DHT11_Read() 호출
        - 읽기 성공 시 온도/습도 출력
        - 실패 시 재시도
        - 2초 대기
